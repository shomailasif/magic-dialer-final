"use strict";const assert=require("node:assert/strict"),fs=require("node:fs"),path=require("node:path");const root=path.resolve(__dirname,"..","..",".."),route=fs.readFileSync(path.join(root,"src","app","api","engine","ai","chat","route.ts"),"utf8");assert.match(route,/PRIMARY_MODEL="openai\/gpt-oss-120b"/);assert.match(route,/FALLBACK_MODEL="openai\/gpt-oss-20b"/);assert.match(route,/new Set\(\[429,500,502,503,504\]\)/);assert.match(route,/a\.status===401/);assert.match(route,/a\.status===403\|\|TRANSIENT\.has\(a\.status\)/);assert.match(route,/AbortSignal\.timeout\(3200\)/);assert.match(route,/upstreamStatus:a\.status/);assert.match(route,/GROQ_API_KEY is not configured/);console.log("AI gateway resilience contract: PASS");
