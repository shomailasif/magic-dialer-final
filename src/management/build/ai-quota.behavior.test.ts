import assert from"node:assert/strict";import{quotaPlan}from"../../lib/ai-quota";process.env.AI_CHAT_USER_RPM="2";process.env.AI_CHAT_DEVICE_RPM="1";process.env.AI_CHAT_GLOBAL_RPM="3";process.env.AI_CHAT_GLOBAL_DAILY_UNITS="100";const n=new Date("2026-09-20T12:00:30Z"),p=quotaPlan("chat",{userId:"u1",deviceId:"d1"},40,n);assert.equal(p.length,4);assert.equal(p[0].limit,1);assert.equal(p[1].limit,2);assert.equal(p[2].limit,3);assert.equal(p[3].limit,100);assert.equal(p[3].units,40);assert.equal(p[0].retryAfter,30);assert.match(p[0].scopeKey,/device:d1/);assert.match(p[1].scopeKey,/user:u1/);assert.equal(p[2].scopeKey,"chat:global:minute");assert.equal(p[3].scopeKey,"chat:global:day-units");console.log("AI quota policy behavior: 10/10 checks PASS");
